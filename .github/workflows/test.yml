name: test

on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - run: touch foo.snap

      - name: Upload snap artifact
        uses: actions/upload-artifact@v2
        with:
          name: snap
          path: '*.snap'